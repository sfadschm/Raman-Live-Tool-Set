Data Evaluatin for PL- and Raman-Maps
  based on the "Raman Tool Set" by Patrizio Candeloro

Raman Toolset 2.0
  1 Usage
      Reads XY-WI data from a file or live feed for 2D and 3D display and provide basic evaluation utilities.

  2 Version Log
      v10.0
        Intial creation.
      v10.1
        Add option to limit maximum intensity display (cut-off) to suppress spikes in map generation.
        Remove keyboard connection with interface buttons.
      v10.2
        Fix axes for map export.
      v10.2.1
        Add Peak-in-Range evaluation method.
      v10.2.2
        Ignore (non-numeric) header lines in source files.